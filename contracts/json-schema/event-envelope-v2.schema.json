{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aliagent.local/contracts/json-schema/event-envelope-v2.schema.json",
  "title": "P5 Event Envelope v2",
  "type": "object",
  "required": ["eventId", "eventType", "eventVersion", "occurredAt", "tenantId", "traceId", "producer", "payload"],
  "properties": {
    "eventId": { "type": "string", "format": "uuid" },
    "eventType": { "type": "string", "minLength": 1 },
    "eventVersion": { "const": 2 },
    "occurredAt": { "type": "string", "format": "date-time" },
    "tenantId": { "type": "string", "minLength": 1 },
    "traceId": { "type": "string", "minLength": 1 },
    "producer": { "type": "string", "minLength": 1 },
    "payload": { "type": "object" }
  },
  "additionalProperties": false
}
