asyncapi: 3.0.0
info:
  title: AliAgent AI Reply Requested Event
  version: 2.0.0
channels:
  ai.reply.requested.v2:
    address: ai.reply.requested.v2
    messages:
      AIReplyRequested:
        $ref: '#/components/messages/AIReplyRequested'
operations:
  publishAIReplyRequested:
    action: send
    channel:
      $ref: '#/channels/ai.reply.requested.v2'
    messages:
      - $ref: '#/channels/ai.reply.requested.v2/messages/AIReplyRequested'
components:
  messages:
    AIReplyRequested:
      name: AIReplyRequested
      contentType: application/json
      payload:
        allOf:
          - $ref: ../json-schema/event-envelope-v2.schema.json
          - type: object
            properties:
              eventType:
                const: AIReplyRequested
              producer:
                const: conversation-service
              payload:
                $ref: ../json-schema/ai-reply-requested-v2.schema.json
